
`tuple(可迭代的对象)`
```python
a = tuple()     # 创建一个空元组对象
b = tuple("abc")
c = tuple(range(3))
d = tuple([2, 3, 4])
```
> **总结**:
> `tuple()` 可以接收列表、字符串、其他序列类型、迭代器等生成元组。
> `list()` 可以接收元组、字符串、其他序列类型、迭代器等生成列表。

___
## 元组的元素访问和计数

- 元组的元素不能修改

```python
a = (20, 10, 30, 9, 8)
a[3] = 33
```
